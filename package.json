{
  "name": "lyric-video-generator-root",
  "version": "0.1.2",
  "private": true,
  "scripts": {
    "install": "cd web && npm install",
    "build": "cd web && npm run build",
    "web:dev": "cd web && npm run dev",
    "api:start": "python -m src.api.main"
  }
}
